/// @description Insert description here
// You can write your code in this editor
if (visible) {
	draw_self();
	if (die == -1) {
		draw_text(x + 4, y - string_height("Upgrade Die"), "Upgrade Die"); 
	}
}